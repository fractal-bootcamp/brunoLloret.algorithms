
export function linearSearch(array: number[], input: number) {
    const x = input

    for (let i = 0; i < array.length; i++) {
        if (x === array[i]) {
            return array[i]
        }

        else

            return false

    }
}

